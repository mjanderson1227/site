import { defineAction } from "astro:actions";
import { loginSchema, registerSchema } from "@/validators/zod";
import { z } from "astro:schema";
import { auth } from "@/lib/auth";

export const server = {
  /* TODO: Fill in server side for blog posts */
};
